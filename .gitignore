# Python-generated files
__pycache__/
.ipynb_checkpoints/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
.worktrees
queue/

# Results — track summaries and key visuals, not raw logs
results/*.tsv
results/logs_*/
results/sweep_5min/
results/mps_pretest/*.log

# Viz — all viz content (scripts + generated PNGs)
viz/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results files (per-run TSVs live under results/)
results.tsv
*.png
!progress.png
!h2h_relative_improvement.png

# Experiment checkpoints & logs
*.best
run.log

# Director binaries, logs & secrets
director/logs/
*/director
.env
.director_prefetch.json

# Native build output (rebuild on target machine)
native/build/

# CoreML model artifacts
*.mlpackage
*.mlmodelc

# Checkpoint files
*.pt
*.bin
